## 1. Specify behavior first

- [ ] 1.1 Add failing fixtures for clear, overly cultured, abstract, infantilizing and contextually ambiguous vocabulary.
- [ ] 1.2 Add failing tests for lexical-validation precondition and separation of deterministic metadata from LLM judgment.

## 2. Implement the evaluator

- [ ] 2.1 Implement metadata checks plus vocabulary-specific structured assessment, verifying sources/confidence are distinct.
- [ ] 2.2 Integrate per-word diagnostics and aggregate score, verifying allowed words can still receive soft warnings.

## 3. Verify

- [ ] 3.1 Run `npm test` and `npm run build`, verifying audience-level fixtures pass.
